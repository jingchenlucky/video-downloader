## Agent skills

### Issue tracker

Issues and PRDs live as local markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles mapped to `Status:` lines in issue files. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo: `CONTEXT.md` at repo root + `docs/adr/`. See `docs/agents/domain.md`.
